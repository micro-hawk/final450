#include<bits/stdc++.h>
using namespace std;

int main()
{
	#ifndef ONLINE_JUDGE
    freopen ("input.txt", "r", stdin);
    freopen ("output.txt", "w", stdout);
	#endif

    int x,y,z; cin >> x >> y >> z;
    int arr[3] = {x,y,z};
    for (int i=0; i<3; i++)
    {
    	for (int j = i+1; i < 3; i++)
    	{
    		
    	}
    }
	return 0;
}